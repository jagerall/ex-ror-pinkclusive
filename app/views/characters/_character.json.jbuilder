json.extract! character, :id, :name, :surname, :pseudonym, :age, :pronoun, :nationality, :job, :capacity, :fact, :personality, :story, :image, :created_at, :updated_at
json.url character_url(character, format: :json)
